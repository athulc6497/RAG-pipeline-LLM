# RAG-GEN-AI
Developed a Retrieval-Augmented Generation (RAG) pipeline using LangChain, Sentence Transformers (all-MiniLM-L6-v2), and FAISS. Implemented document ingestion, parsing, chunking, embedding generation, semantic retrieval, and LLM-based context-aware response generation to deliver accurate answers from custom document datasets.
